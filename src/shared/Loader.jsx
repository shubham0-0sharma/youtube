import React from "react";

// * a loader component which will be shared across all the component whenever loading state is true

const Loader = () => {
    <div className="load-bar">
        <div className="bar"></div>
        <div className="bar"></div>
        <div className="bar"></div>
    </div>;
};

export default Loader;
